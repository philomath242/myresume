1. > **latex-resume** has the tex files for rendering resume in a LaTeX compiler  





2. > Change to React application directory
`cd react-resume `
Install Node.js dependencies
`npm install `
Run application
`npm run dev`
